services:
  osrs-bingo:
    build: .
    container_name: osrs-bingo
    ports:
      - "3100:3001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
      - PORT=3001
    env_file:
      - .env
    restart: unless-stopped
